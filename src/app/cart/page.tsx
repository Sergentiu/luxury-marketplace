import { CartPage } from "@/components/cart/cart-page";

export default function Cart() {
  return <CartPage />;
}

export const metadata = {
  title: "Shopping Cart | Luxury Marketplace",
  description: "Review your luxury items before checkout.",
};
