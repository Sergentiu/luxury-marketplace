import { AccountPage } from "@/components/account/account-page";

export default function Account() {
  return <AccountPage />;
}

export const metadata = {
  title: "My Account | Luxury Marketplace",
  description: "Manage your account, orders, and preferences.",
};
